specVersion: "v1"
serviceId: "musicplayer"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "musicplayer-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
